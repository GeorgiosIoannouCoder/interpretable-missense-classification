# Core ML
# Install torch from PyTorch's CUDA 12.6 index so it matches the GH200's
# 12.8 driver. The PyPI default (torch+cu130) requires a newer driver.
# Run BEFORE `pip install -r requirements.txt`:
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
torch>=2.3,<3.0
transformers>=4.40,<5.0
accelerate>=0.30,<2.0
scikit-learn>=1.4,<2.0

# Bioinformatics
biopython>=1.83,<2.0
pysam>=0.22,<1.0

# Data
pandas>=2.2,<3.0
pyarrow>=15.0,<22.0
numpy>=1.26,<3.0
scipy>=1.12,<2.0
h5py>=3.10,<4.0

# Visualization
matplotlib>=3.8,<4.0
seaborn>=0.13,<1.0
umap-learn>=0.5,<1.0

# Utilities
joblib>=1.3,<2.0
tqdm>=4.66,<5.0
pyyaml>=6.0,<7.0
requests>=2.31,<3.0

# Dev / test
pytest>=8.0,<9.0
ruff>=0.4,<1.0
black>=24.0,<26.0
